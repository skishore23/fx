{
  "name": "@fx/examples",
  "version": "1.0.0",
  "description": "Examples for the Fx Framework",
  "private": true,
  "scripts": {
    "coding-agent": "cd coding-agent && npm start",
    "coding-agent:test": "cd coding-agent && npm test",
    "coding-agent:complex": "cd coding-agent && npm run test:complex",
    "research-agent": "cd research-agent && npm start",
    "research-agent:test": "cd research-agent && npm test",
    "all": "npm run coding-agent && npm run research-agent",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fx/core": "file:../packages/fx-core",
    "zod": "^3.22.4",
    "openai": "^4.98.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1"
  }
}
